import React from 'react';

const Home = () => {
    const appName = "TrackTrainer";

    return (
        <div id="home">
            <h1>Bienvenido a {appName}</h1>
        </div>
    );
};

export default Home;
