import React from 'react';

const Home = () => {
    const appName = "TrackTrainer";

    return (
        <div>
            <h1>Bienvenido a {appName}</h1>
        </div>
    );
};

export default Home;
